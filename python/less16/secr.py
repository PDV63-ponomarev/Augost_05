# import mysql.connector as mysql
# from /project/Augost_05/python/less16 import creds
#
# db = mysql.connect(
#     user = credits.user,
#     passwd = credits.passwd,
#     host = credits.host,
#     port = credits.port,
#     database = credits.database
# )
#
# cursor = db.cursor(dictionary=True)
# cursor.execute('SELECT * FROM students')
# data = cursor.fetchall()
# print(data)
