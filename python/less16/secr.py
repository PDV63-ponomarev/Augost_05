# import mysql.connector as mysql


# from Augost_05.python.less16 import creds

# db = mysql.connect(
#     user = creds.user,
#     passwd = creds.passwd,
#     host = creds.host,
#     port = creds.port,
#     database = creds.database
# )

# cursor = db.cursor(dictionary=True)
# cursor.execute('SELECT * FROM students')
# data = cursor.fetchall()
# print(data)
